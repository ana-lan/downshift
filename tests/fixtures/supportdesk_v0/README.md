# SupportDesk v0 (frozen)

A frozen copy of `examples/supportdesk/supportdesk/` from before Phase 7, when model
names were hard-coded, read from env vars, or hidden in dicts and kwargs.

The scanner and resolver tests run against this copy so they keep testing those
patterns after the live demo app moved its model names into `models.yaml`.
Do not edit it.
